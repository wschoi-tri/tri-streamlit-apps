# --- Deep Learning Core ---
# PyTorch CPU version index url for Windows/Linux CPU environment
--extra-index-url https://download.pytorch.org/whl/cpu
# PyTorch: 딥러닝 프레임워크
torch>=2.2.0

# Transformers: BERT 모델 사용 (Hugging Face)
transformers>=4.41.2

# Accelerate: PyTorch 모델 학습 및 추론 가속화 (Transformers 권장 의존성)
accelerate>=0.30.1

# --- Data Processing & Machine Learning ---
# Scikit-learn: PCA 차원 축소용
scikit-learn>=1.4.0

# Pandas: CSV 데이터 처리å
pandas>=2.2.0

# NumPy: 수치 연산 (Pandas/Scikit-learn 의존성)
numpy>=1.26.0

# --- Vector Search ---
# FAISS: 벡터 검색 엔진 (CPU 버전)
faiss-cpu>=1.7.4

# Milvus SDK: Zilliz/Milvus 데이터베이스 연동
pymilvus>=2.6.12

# Elasticsearch SDK: Elasticsearch 데이터베이스 연동
elasticsearch>=9.4.0


# --- Backend API ---
# FastAPI: 고성능 API 프레임워크
fastapi>=0.128.0

# Uvicorn: ASGI 웹 서버 (FastAPI 구동용)
uvicorn>=0.40.0

# Pydantic: 데이터 유효성 검사 (FastAPI 의존성 명시)
pydantic>=2.12.5

# --- Frontend UI ---
# Streamlit: 데이터 테스트용 웹 UI
streamlit>=1.31.0

# --- Additional Utilities & Embeddings ---
# Sentence-Transformers: 텍스트 임베딩 모델 사용 (jhgan/ko-sroberta-multitask 등)
sentence-transformers>=2.7.0

# tqdm: 진행률 표시 바
tqdm>=4.66.0

# python-dotenv: .env 파일에서 환경 변수 로드
python-dotenv>=1.2.1

# Hanja: 한자-한글 변환 라이브러리 (필수)
hanja>=0.5.0

# Joblib: 데이터셋 로드 병렬화
joblib>=1.5.1
